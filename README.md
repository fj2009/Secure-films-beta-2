# Secure-films-beta-2
Gestor de archivos cifrados por terminal que crea un secure_store, gestiona usuarios y permisos, y permite cifrar/descifrar archivos con acceso controlado. Funciona con scripts start_*/install_* y no necesita dependencias externas.
